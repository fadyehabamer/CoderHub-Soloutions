function DecimalPlaces(num) {
  // return the number of digits after the decimal point
  // let x = num.toString().split('.')[1].length || 0;
  console.log(5.0 == 5);
  // return parseInt(x);
}
console.log(DecimalPlaces(5.0));